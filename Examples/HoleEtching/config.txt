# Config file for a hole etching example process
# all size values are given in micrometers

# Domain
gridDelta=0.05
xExtent=1
yExtent=1

# Geometry
holeRadius=0.2
maskHeight=0.2
taperAngle=0

# Process paramters
processTime=100 # seconds

# all flux values are units 1e15 / cm²
ionFlux=10.
etchantFlux=2000.
oxygenFlux=1000.

rfBias=100 # rf plasma bias (W) controls the ion energy distribution
A_O=3 # passivation layer sputtering coefficient

raysPerPoint=100