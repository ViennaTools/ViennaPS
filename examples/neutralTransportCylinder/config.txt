# Neutral transport benchmark on a 3D cylinder geometry

# Units
lengthUnit=um
timeUnit=s

# Domain
gridDelta=0.005
xExtent=0.15
yExtent=0.15

# Geometry
holeRadius=0.05
maskHeight=1.
maskTaperAngle=0.0

# Process
processTime=0.01
fluxEngine=GT
raysPerPoint=800
spatialScheme=EO_2
temporalScheme=FE
calculateIntermediateVelocities=false

# Neutral transport parameters
# Fixed-pressure Maxwellian reservoir boundary. The source flux is computed as
# Gamma_source = p / sqrt(2*pi*m*kB*T).
sourcePressure=1.0
sourceTemperature=300.0
sourceMolecularMass=18.998
zeroCoverageSticking=0.0
etchFrontSticking=1.0
desorptionRate=50.0
desorptionMaterial=Mask
kEtch=50.0
surfaceSiteDensity=1.66e-5
siliconDensity=8.3e4
coverageTimeStep=0.01
useSteadyStateCoverage=true
# 5e-9 m^2/s = 5e3 um^2/s
surfaceDiffusionCoefficient=5000.0
surfaceDiffusionMaterial=Mask
# 0.0 picks a neighbor distance from the surface point spacing.
surfaceDiffusionNeighborDistance=0.0
surfaceDiffusionSolverTolerance=1e-8
surfaceDiffusionMaxIterations=500
sourceExponent=1.0

# Coverage initialization
coverageInitIterations=8
coverageTolerance=1e-4

# Input
initialFile=neutralTransportCylinder_initial.vtp
# Output
outputFile=neutralTransportCylinder_final.vtp
